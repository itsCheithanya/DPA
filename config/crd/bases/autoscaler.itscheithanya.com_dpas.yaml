---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: dpas.autoscaler.itscheithanya.com
spec:
  group: autoscaler.itscheithanya.com
  names:
    kind: DPA
    listKind: DPAList
    plural: dpas
    singular: dpa
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DPA is the Schema for the dpas API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DPASpec defines the desired state of DPA.
            properties:
              deploymentName:
                type: string
              namespace:
                type: string
              scrapeURL:
                type: string
            required:
            - deploymentName
            - namespace
            - scrapeURL
            type: object
          status:
            description: DPAStatus defines the observed state of DPA.
            properties:
              currentReplicas:
                format: int32
                type: integer
              lastScaleTime:
                description: |-
                  INSERT ADDITIONAL STATUS FIELD - define observed state of cluster
                  Important: Run "make" to regenerate code after modifying this file
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
